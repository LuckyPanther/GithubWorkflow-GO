{
  "name": "Go Environnement",
  "dockerFile": "../Dockerfile", // Chemin relatif correct
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ]
    }
  },
  "postCreateCommand": "go mod init mymodule && go mod tidy", // Commandes pour initialiser les modules Go
  "remoteUser": "vscode" // Utilisateur défini dans le Dockerfile
}

  
  